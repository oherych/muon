name: tests

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    strategy:
      matrix:
        go-version: [1.17.x, 1.18.x, 1.19.x]
        arch: [arm, ia32, x32, x64]

    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          architecture: ${{ matrix.arch }}
          go-version: ${{ matrix.go-version }}

      - name: Test
        run: go test -v ./...